# Core data processing
pandas>=1.5.0
numpy>=1.21.0
openpyxl>=3.0.9

# HTTP requests and API interaction
requests>=2.28.0
urllib3>=1.26.0

# Environment and configuration management
python-dotenv>=0.19.0

# Data visualization (optional, for analysis)
matplotlib>=3.5.0
seaborn>=0.11.0

# Google AI/Gemini integration
google-generativeai>=0.3.0

# Command line argument parsing (built-in, but noting for clarity)
# argparse is part of Python standard library

# Logging and utilities (built-in, but noting for clarity)
# logging, pathlib, datetime, collections are part of Python standard library

# Optional: Progress bars and better CLI experience
tqdm>=4.64.0

# Optional: Better JSON handling
ujson>=5.0.0 